[tool.ruff]
target-version = "py311"
line-length = 100
src = ["app", "config", "pipeline", "src", "utils"]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "N", "UP", "B", "C4"]
ignore = ["E501"]

[tool.ruff.lint.isort]
known-first-party = ["app", "config", "pipeline", "src", "utils"]
